// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default). Framing happens in the browser via canvas.
export default defineConfig({
  site: 'https://lilmockup.netlify.app',
});
